

        <div class="inner cover">
          <h1 class="title-header"><i class="fa fa-droplet me-4"></i>{{title}}</h1>

          <div class="row">
            <div class="col-lg-4">
              {{> a1c }}
            </div>
            <div class="col-lg-4">
              {{> glucose-reading }}
            </div>
            <div class="col-lg-4">
              {{> food-entry }}
            </div>
          </div>
          {{!-- <div class="row">
            <div class="col-lg-12"></div>
          </div> --}}
            
          <div class="row">
            <div class="col-lg-6">
              {{> glucose-chart }}
            </div>
            <div class="col-lg-6">
              {{> weight-chart }}
            </div>
          </div>
        </div>

        {{> menu-button }}

        {{!-- include the partial --}}
        {{> glucose-dialog }}
        {{> food-dialog }}
        {{> weight-dialog}}

        {{#section "head"}}
        <script src="https://cdn.jsdelivr.net/npm/chart.js?path=dist"></script>
        <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns"></script>
        {{/section}}

        {{#section "scripts"}}
        {{/section}}